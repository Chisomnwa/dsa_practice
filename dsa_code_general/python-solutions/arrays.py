new_list = [1, 2, 3]

# Accessing an Array
result = new_list[0]
print(result)

# Searching for an arbitral value in an array
# using an if srtatement
if 1 in new_list: print(True)

# Using a for loop
for n in new_list:
    if n == 1:
        print(True)
    break